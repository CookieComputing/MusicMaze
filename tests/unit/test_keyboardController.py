from unittest import TestCase


class TestKeyboardController(TestCase):
    """This class tests that the implementation of a keyboard controller will
    call specific functions as displayed by the interface for the
    model and view. Any subsequent tests in this class should be used in
    conjunction with a dummy model and view to determine that the view and
    model functions are actually being called appropriately."""

    pass
