class MusicMaze:
    """This class represents an instance of a music maze game. The class itself
    is responsible for constructing a graph such that the graph is now
    interpreted as a """

    def __init__(self, width, height, seed):
        pass

    def __str__(self):
        """Represent the maze through a string. The maze itself should follow
        the format whereby each of the cells are separated by three spaces,
        and if the cells are connected, they will be joined in the middle by
        a separator character defined below.

        Returns:
            str: a string representation of the maze"""
        return ""
